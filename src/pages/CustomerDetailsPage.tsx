import { Card } from "../components/ui/Card";

export function CustomerDetailsPage() {
  return (
    <section className="space-y-5">
      <h2 className="text-2xl font-bold text-[var(--pos-text)]">Customer Details</h2>
      <Card className="p-5 text-sm text-[var(--pos-muted)]">Customer profile details will be added in a later step.</Card>
    </section>
  );
}
